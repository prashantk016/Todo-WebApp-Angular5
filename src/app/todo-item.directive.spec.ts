import { TodoItemDirective } from './todo-item.directive';

describe('TodoItemDirective', () => {
  it('should create an instance', () => {
    const directive = new TodoItemDirective();
    expect(directive).toBeTruthy();
  });
});
